<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.nju.hw.mapper.OrderMapper">
 	<!-- 增加一个order -->  
    <insert id="addOrder"> 
        insert into myOrder(vid,hid,startDate,endDate,bed,price,num,name) values(#{0},#{1},#{2},#{3},#{4},#{5},#{6},#{7})  
     </insert>
     
     <!-- 查找vid的所有订单 -->
     <select id="getMyOrders" resultMap="orderResultMap" resultType="Order"></select>
     

</mapper>