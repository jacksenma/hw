function checkForm(fid) {
	//TODO增加对手机号和银行卡号的格式校验
	alert("a");
	alert(fid);
	alert($("#"+fid).serialize());
	alert("a");

//	toastr.success('取消预订成功');
//	setTimeout(function() { $("#sub").click() }, 1000);
}
