function test(){
//	var data=[];
	$.ajax({
        type: "get",
        url: "http://localhost:8080/hw/getAllHchecks",
        
        success: function(data){
//        	alert(data);
//        	alert(data.length);
//        	alert(data);
//        	alert(eval("("+data+")"));
//        	var json=data;
//        	alert(eval("({"+data+"})"));
//        	var d=eval("("+data+")");
        	
        	var da=eval("("+data+")");
        	alert(da.length);
        	
//        	alert(data.size);
        	for(key in da.checks)
        		alert(da.checks[key].date);
        	
//        	alert(d.checks[1].date);
//        	for(var o in d[0]){  
//                alert(o);  
//                alert(d[o]);  
//                alert("text:"+d[o].date);  
//              }  
//        	json = eval(json.p)  
//        	  for(var i=0; i<data.length; i++){  
//        	     alert(data.checks[i].date+" " + data.checks[i].orderNum+" "+data.checks[i].cancelNum+" " + data.checks[i].enterNum+" "+data.checks[i].leaveNum)  
//        	  }  
//        	}  
//     	   if(data=="yes")
//     		   toastr.warning('用户名已存在');
//     	   else{
//     		   $("#sub").click();
//     	   }
           }
     });
}